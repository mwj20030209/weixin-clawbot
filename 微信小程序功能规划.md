# 微信小程序功能规划文档

## 一、产品定位

**AI 智能聊天小程序** —— 用户在微信小程序内直接与 AI 对话，支持多角色人设、长期记忆，同时提供微信个人号 Bot 绑定功能，实现"小程序 + 微信私聊"双通道 AI 陪伴。

---

## 二、功能模块总览

| 模块 | 优先级 | 说明 |
|------|--------|------|
| 微信登录与用户体系 | P0 | 小程序原生微信登录，自动创建用户 |
| AI 聊天（核心） | P0 | 小程序内实时对话，支持多会话 |
| 人设角色市场 | P0 | 选择/切换 AI 人设角色 |
| 额度与支付 | P0 | 小程序内微信支付购买额度 |
| 微信 Bot 绑定 | P1 | 扫码绑定个人微信号，AI 通过私聊触达 |
| 聊天记录与记忆 | P1 | 历史消息、AI 长期记忆展示 |
| 邀请奖励 | P2 | 分享小程序卡片邀请好友得额度 |
| 个人中心 | P2 | 账户信息、设置、反馈 |

---

## 三、各模块详细功能

### 3.1 微信登录与用户体系（P0）

**前端**：
- 首次进入调用 `wx.login()` 获取 code
- 后端用 code 换取 openid/unionid，自动注册或登录
- 登录后获取 JWT token，后续请求携带

**后端新增**：
- `POST /api/auth/wx-login` — 接收 code，返回 token + 用户信息
- users 表新增 `openid`, `unionid` 字段
- 小程序用户默认 role = 'user'

**关键点**：
- 无需手动注册，微信一键静默登录
- unionid 用于后续与公众号/Bot 账号打通

---

### 3.2 AI 聊天（核心功能 P0）

**前端页面**：
- **会话列表页**：展示所有聊天会话（按角色分组），最新消息预览
- **聊天页**：仿微信聊天 UI，支持文字输入、AI 流式回复（SSE）
- 显示"AI 正在输入…"状态
- 支持长按复制消息
- 底部输入栏 + 发送按钮

**后端新增**：
- `POST /api/chat/send` — 发送消息，触发 AI 回复
- `GET /api/chat/history/:sessionId` — 获取历史消息（分页）
- `POST /api/chat/sessions` — 创建新会话（指定角色）
- `GET /api/chat/sessions` — 获取用户所有会话列表
- `DELETE /api/chat/sessions/:id` — 删除会话

**数据表**：
```sql
-- 会话表
CREATE TABLE chat_sessions (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  persona_id INT,              -- 使用的角色ID
  title VARCHAR(100),          -- 会话标题（AI自动生成）
  last_message TEXT,           -- 最近一条消息预览
  created_at DATETIME DEFAULT NOW(),
  updated_at DATETIME DEFAULT NOW() ON UPDATE NOW()
);

-- 消息表
CREATE TABLE chat_messages (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  session_id INT NOT NULL,
  role ENUM('user', 'assistant') NOT NULL,
  content TEXT NOT NULL,
  created_at DATETIME DEFAULT NOW(),
  INDEX idx_session_time (session_id, created_at)
);
```

**核心逻辑**：
- 复用现有 `callAI` + 三段 Prompt 拼接（人设 → 长期记忆 → 上下文）
- 每次对话消耗 1 额度
- 超出上下文长度自动摘要归档到长期记忆
- 回复后异步提取用户信息写入记忆

---

### 3.3 人设角色市场（P0）

**前端页面**：
- **角色列表页**：卡片式展示可用角色（头像、名字、简介、标签）
- **角色详情**：完整介绍 + "开始聊天"按钮
- 标记"官方推荐" / "热门" / "新上架"

**后端**：
- 复用现有 `personas` 表
- `GET /api/personas/public` — 获取公开角色列表（小程序端）
- 支持标签筛选、排序

**角色类型**：
- 官方预设角色（系统提供）
- 用户自定义角色（后续 P2 支持）

---

### 3.4 额度与微信支付（P0）

**前端页面**：
- **额度展示**：首页/个人中心显示剩余额度
- **充值页**：套餐卡片选择 → 微信支付
- 支付成功动画 + 额度刷新

**后端**：
- `POST /api/orders` — 创建订单（payType = 'jsapi'）
- `POST /api/wx-pay/notify` — 支付回调（已有）
- 小程序支付流程：后端下单 → 返回支付参数 → 前端 `wx.requestPayment()` → 回调确认

**关键点**：
- 复用现有 packages 表和订单逻辑
- 小程序支付需要用户 openid 参与签名
- 需在微信支付商户平台配置小程序 appid

---

### 3.5 微信 Bot 绑定（P1）

**功能说明**：
用户在小程序中扫码（或输入码）绑定自己的微信个人号，绑定后 AI 可通过微信私聊主动联系用户（早安问候、定时消息等）。

**前端页面**：
- **绑定引导页**：说明绑定好处 + 绑定步骤
- **绑定状态页**：已绑定/未绑定，支持解绑
- 绑定方式：小程序内展示 Bot 二维码，用户用微信扫码添加好友

**后端新增**：
- `POST /api/bindbot/bindcode` — 生成绑定码（关联 userId + botId）
- `POST /api/bindbot/confirm` — Bot 收到用户消息后确认绑定关系
- `GET /api/bindbot/status` — 查询当前绑定状态

**数据表**：
```sql
CREATE TABLE user_bot_binddings (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  bot_id VARCHAR(100) NOT NULL,    -- Bot 的微信ID
  wx_from_id VARCHAR(100) NOT NULL, -- 用户在Bot视角的fromId
  status ENUM('active','unbound') DEFAULT 'active',
  bound_at DATETIME DEFAULT NOW(),
  UNIQUE KEY uk_user_bot (user_id, bot_id)
);
```

**绑定流程**：
1. 用户在小程序点击"绑定微信"
2. 展示 Bot 的微信二维码 + 绑定口令（如 `#BIND:ABC123`）
3. 用户扫码添加 Bot 好友，发送绑定口令
4. Bot 收到口令后验证，自动关联 user_id ↔ from_id
5. 绑定成功，用户在小程序看到状态变更

**绑定后能力**：
- Bot 主动推送定时消息到用户微信
- 微信内对话与小程序共享记忆和人设
- 微信对话也消耗同一额度池

---

### 3.6 聊天记录与记忆（P1）

**前端**：
- 会话内消息无限滚动加载
- 可查看 AI 对用户的"记忆印象"（可选展示）

**后端**：
- 复用现有 `tb_long_memory` + `tb_base_role` 表
- 小程序对话产生的记忆与微信 Bot 对话记忆统一存储
- `GET /api/memory/me` — 用户查看 AI 对自己的记忆摘要

---

### 3.7 邀请奖励（P2）

**前端**：
- 个人中心展示邀请码/小程序分享卡片
- 邀请记录列表
- 奖励到账提示

**后端**：
- 复用现有 `invite_code` + `invite_count` + `invite_by` 逻辑
- 小程序分享时携带 inviterCode 参数
- 新用户注册时自动绑定邀请关系、双方加额度

---

### 3.8 个人中心（P2）

- 头像昵称（微信授权获取）
- 剩余额度 + 充值入口
- 微信 Bot 绑定状态
- 邀请好友入口
- 使用记录/账单
- 意见反馈
- 关于我们

---

## 四、技术架构

```
┌─────────────────────────────────────────────────┐
│              微信小程序（前端）                    │
│  Taro / UniApp / 原生微信小程序                   │
│  页面：聊天、角色、充值、绑定、个人中心            │
└────────────────────┬────────────────────────────┘
                     │ HTTPS
                     ▼
┌─────────────────────────────────────────────────┐
│              Node.js 后端（现有 server.js 扩展）   │
│                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ 用户认证  │ │ AI聊天   │ │ 微信支付(JSAPI)  │ │
│  │ wx.login │ │ callAI   │ │ 小程序下单签名   │ │
│  └──────────┘ └──────────┘ └──────────────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ Bot绑定  │ │ 人设管理  │ │ 额度/订单/邀请   │ │
│  └──────────┘ └──────────┘ └──────────────────┘ │
│                                                   │
│  ┌─────────────────────────────────────────────┐ │
│  │  MySQL（用户/会话/消息/记忆/订单/绑定）       │ │
│  └─────────────────────────────────────────────┘ │
└────────────────────┬────────────────────────────┘
                     │ iLink 协议
                     ▼
┌─────────────────────────────────────────────────┐
│         微信 Bot 实例（已有 BotInstance）          │
│  长轮询收发消息、定时推送、重连管理               │
└─────────────────────────────────────────────────┘
```

---

## 五、开发阶段规划

### 第一阶段（MVP 核心，2-3 周）
- [ ] 微信小程序项目初始化
- [ ] 微信静默登录 + JWT 鉴权
- [ ] AI 聊天核心（发消息/收回复/历史记录）
- [ ] 角色选择（复用现有 personas）
- [ ] 额度展示 + 小程序微信支付

### 第二阶段（体验增强，1-2 周）
- [ ] 微信 Bot 扫码绑定流程
- [ ] Bot 对话与小程序记忆打通
- [ ] 聊天体验优化（流式输出/打字动画）
- [ ] 邀请分享（小程序卡片分享）

### 第三阶段（运营完善，1 周）
- [ ] 个人中心完整页面
- [ ] 新手引导 / 使用教程
- [ ] 数据统计与分析
- [ ] 小程序审核上线

---

## 六、与现有后端的复用关系

| 现有能力 | 小程序复用方式 |
|---------|--------------|
| `callAI` + 三段 Prompt | 直接复用，小程序聊天走同一 AI 调用 |
| `personas` 表 | 直接复用，新增公开查询接口 |
| `packages` + 订单 | 直接复用，payType 区分支付方式 |
| `tb_long_memory` | 直接复用，小程序与 Bot 共享记忆池 |
| `tb_base_role` | 直接复用，人设进化对双通道生效 |
| `users` 表 | 扩展 openid/unionid 字段 |
| 微信支付回调 | 直接复用现有回调接口 |
| 邀请码逻辑 | 直接复用，小程序分享携带邀请参数 |

---

## 七、需要新增的后端接口汇总

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/wx-login` | 小程序微信登录 |
| GET | `/api/chat/sessions` | 获取会话列表 |
| POST | `/api/chat/sessions` | 创建会话 |
| DELETE | `/api/chat/sessions/:id` | 删除会话 |
| POST | `/api/chat/send` | 发送消息并获取 AI 回复 |
| GET | `/api/chat/history/:sessionId` | 获取聊天历史（分页） |
| GET | `/api/personas/public` | 获取公开角色列表 |
| POST | `/api/bindbot/bindcode` | 生成绑定码 |
| POST | `/api/bindbot/confirm` | 确认绑定 |
| GET | `/api/bindbot/status` | 查询绑定状态 |
| GET | `/api/memory/me` | 用户查看自己的 AI 记忆 |

---

## 八、小程序页面结构

```
pages/
├── index/            # 首页（会话列表 + 快捷入口）
├── chat/             # 聊天页面
├── roles/            # 角色市场
├── role-detail/      # 角色详情
├── recharge/         # 充值页面
├── bind-bot/         # Bot 绑定页面
├── invite/           # 邀请好友页面
├── profile/          # 个人中心
└── about/            # 关于我们
```

---

## 九、注意事项

1. **小程序审核**：AI 聊天类小程序需要 ICP 备案 + 增值电信许可证（部分类目），提前准备
2. **内容安全**：需接入微信内容安全 API（`msgSecCheck`），AI 回复前/后做敏感词过滤
3. **支付资质**：小程序微信支付需要主体为企业，个人主体无法接入
4. **域名备案**：后端 API 域名需 HTTPS + ICP 备案，在小程序管理后台配置合法域名
5. **AI 回复时长**：小程序网络请求默认 60s 超时，AI 回复慢时需做 loading 提示
6. **unionid 打通**：如需 Bot 绑定打通身份，需要小程序与公众号关联同一开放平台账号
